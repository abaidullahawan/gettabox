<!-- Main Content Header -->
<div class="main-content-header">
  <h1><%= link_to 'System Users', system_users_path, style:'color: #212529;' %></h1>
  <ol class="breadcrumb">
    <li class="breadcrumb-item active">
      <span class="active">Index</span>
    </li>
  </ol>
</div>
<!-- End Main Content Header -->

<div class="row">
  <div class="col-xl-12">
    <div class="card mb-30">
      <div class="card-body">
        <div class="card-header">
          <a href="/system_users/new" class="btn btn-primary float-right">Create</a>
          <h5 class="card-title"> Data</h5>
        </div>
        <%= search_form_for @q, url: system_users_path, method: :get do |f| %>
          <div class='row'>
            <div class='col-2'>
              <button type="submit" name="export_csv" value="ture" class='btn btn-outline-primary'><a href="<%= system_users_path %>"></a>Export</button>
            </div>
            <div class='col-1 offset-9'>
              <label for="inputState">Per Page</label>
              <%= select_tag :limit, options_for_select([5, 10, 15, 20], selected: params[:limit] || 10), id: 'change_per_page', class: 'form-control' %>
              <button type="submit" id="per_page_submit" hidden="hidden"></button>
            </div>
          </div>
          <br>
          <div class="table-responsive">
            <table class="table m-0 table-hover">
              <tr>
                <th><%= sort_link(@q, :id, 'ID') %></th>
                <th class="text-center"><%= sort_link(@q, :sku, 'SKU') %></th>
                <th class="text-center"><%= sort_link(@q, :user_type, 'User Type') %></th>
                <th colspan="3" class="text-center">Actions</th>
              </tr>
              <% @system_users&.each&.with_index(1) do |system_user, index| %>
                <tr class=''>
                  <td><%= system_user.id %></td>
                  <td class="text-center"><%= system_user.sku %></td>
                  <td class="text-center"><%= system_user.user_type %></td>
                  <td class="text-center">
                    <a type="button" class="btn btn-link text-info p-0 mr-2" href="<%= system_user_path(system_user)%>">
                      <i data-feather="eye" class="icon wh-15"></i>
                    </a>
                    <a type="button" class="btn btn-link text-success p-0 mr-2" href="<%= edit_system_user_path(system_user)%>">
                      <i data-feather="edit-2" class="icon wh-15"></i>
                    </a>
                    <%= link_to '<i data-feather="x" class="icon wh-15"></i>'.html_safe, system_user_path(system_user), method: 'delete', data: {confirm: "Are you sure you want to delete"}, class: "btn btn-link text-danger p-0"%>
                  </td>
                </tr>
              <% end %>
            </table>
          </div>
        <% end %>
        <br>
        <div class='float-right'>
          <%= page_entries_info @system_users %>
        </div><br>
        <div class="pagination justify-content-center mt-3">
          <%= paginate @system_users,theme: "twitter-bootstrap-4",pagination_class: "pagination-sm",nav_class: "d-inline-b" %>
        </div>
      </div>
    </div>
  </div>
</div>